{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Odin: Build Project",
            "type": "shell",
            "command": "mkdir -p bin && odin build . -debug -out:bin/my_game",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
        }
    ]
}